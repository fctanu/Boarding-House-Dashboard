import type { Tenant, Room } from "../types";

// Start with no seed data. All tenants, rooms, and payments are empty by default.
export const initialTenants: Tenant[] = [];
export const initialRooms: Room[] = [];
